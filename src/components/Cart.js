import React from "react";

const Cart = (cart) => {
  console.log(cart);
  return (
    <div>
      {/* {cart.map((item) => (
        <p>{item}</p>
      ))} */}
    </div>
  );
};

export default Cart;
